// eslint-disable-next-line no-inline-comments
/*

import { GEO_URL, IPSTACK_URL } from '@gConstants/client/client-constants';
import axios from 'axios';
*/

export const GetGeolocationQuery = async () => {
  return new Promise((resolve) => {
    resolve(null);
  });
  // eslint-disable-next-line no-inline-comments
  /*
  return await axios.get(GEO_URL).then((res) => {
    return res?.data;
  }); */
};

export const GetIpstackQuery = async () => {
  return new Promise((resolve) => {
    resolve(null);
  });
  // eslint-disable-next-line no-inline-comments
  /*
  return await axios.get(IPSTACK_URL).then((res) => {
    return res?.data;
  });
  */
};
