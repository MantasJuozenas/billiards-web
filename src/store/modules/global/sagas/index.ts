import { all } from 'redux-saga/effects';

// import { ApolloClientSaga } from './apollo-client.sagas';

export default function* rootSaga() {
  // yield all([...ApolloClientSaga]);
  yield all([]);
}
